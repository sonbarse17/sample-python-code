apiVersion: v1
kind: Service
metadata:
  name: sample-python-service
  labels:
    app: sample-python-app
spec:
  type: ClusterIP
  selector:
    app: sample-python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
