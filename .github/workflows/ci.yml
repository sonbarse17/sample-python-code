name: Project DevSecOps Pipeline

on:
  push:
    branches:
      - feature
      - main
      - release
  pull_request:
    branches:
      - main
      - release

permissions:
  contents: read
  actions: read
  security-events: write
  packages: write

jobs:
  run-reusable-pipeline:
    uses: sonbarse17/reusable-devops-workflow/.github/workflows/reusable-pipeline.yml@main
    with:
      environment: ${{ github.ref_name == 'main' && 'uat' || (github.ref_name == 'release' && 'prod' || 'dev') }}
      image_name: 'sample-code'
      registry: 'docker.io/sonbarse17'
      k8s_cluster: 'k8s-cluster'
      k8s_namespace: 'default'
      k8s_server: 'https://kubernetes-api-server.example.com'
      deploy_method: 'kubectl'
    secrets:
      REGISTRY_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      REGISTRY_PASSWORD: ${{ secrets.DOCKERHUB_TOKEN }}
      KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
